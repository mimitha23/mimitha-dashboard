import { createSlice } from "@reduxjs/toolkit";

const developedProductsSlice = createSlice({
  name: "developed-products",
  initialState: {},
  reducers: {},
});

export default developedProductsSlice.reducer;
export const {} = developedProductsSlice.actions;
