import { createSlice } from "@reduxjs/toolkit";

const createProductTypeSlice = createSlice({
  name: "create-product-type",
  initialState: {},
  reducers: {},
});

export default createProductTypeSlice.reducer;
export const {} = createProductTypeSlice.actions;
