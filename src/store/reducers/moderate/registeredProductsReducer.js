import { createSlice } from "@reduxjs/toolkit";

const registeredProductsSlice = createSlice({
  name: "registered-products",
  initialState: {},
  reducers: {},
});

export default registeredProductsSlice.reducer;
export const {} = registeredProductsSlice.actions;
