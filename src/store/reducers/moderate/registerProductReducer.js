import { createSlice } from "@reduxjs/toolkit";

const registerProductSlice = createSlice({
  name: "register-product",
  initialState: {},
  reducers: {},
});

export default registerProductSlice.reducer;
export const {} = registerProductSlice.actions;
