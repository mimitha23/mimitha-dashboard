import { createSlice } from "@reduxjs/toolkit";

const createVariantSlice = createSlice({
  name: "create-variant",
  initialState: {},
  reducers: {},
});

export default createVariantSlice.reducer;
export const {} = createVariantSlice.actions;
