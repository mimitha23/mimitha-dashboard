import { createSlice } from "@reduxjs/toolkit";

const addDevelopedProductSlice = createSlice({
  name: "add-developed-product",
  initialState: {},
  reducers: {},
});

export default addDevelopedProductSlice.reducer;
export const {} = addDevelopedProductSlice.actions;
